% Load necessary packages this works for 2024_r4ds-ggplot2-instructor.tex
\usepackage{graphicx}    % For including images
\usepackage{amsmath}     % For advanced math typesetting
\usepackage{hyperref}    % For creating hyperlinks
\usepackage{float}       % For precise float placement
\usepackage{adjustbox}   % For adjusting boxes, including figures and tables
\usepackage{caption}     % For better control over captions
\usepackage{placeins}    % Allows \FloatBarrier command to manage float placement

% Load the geometry package once with desired options
\usepackage[a4paper, margin=1in]{geometry}  % Set 1-inch margins for A4 paper

\usepackage{lscape}      % For landscape orientation (if needed)

\usepackage{adjustbox}
\newcommand{\defaultimage}[1]{\includegraphics[width=\linewidth, keepaspectratio]{#1}}

% Custom commands
\newcommand{\vect}[1]{\boldsymbol{#1}}  % Command to bold vectors

% Custom formatting
\setlength{\parindent}{0pt}             % Remove paragraph indentation
\setlength{\parskip}{1em}               % Add space between paragraphs

% Adjusted float settings to prevent "float too large for page" warnings
\setlength{\textfloatsep}{10pt plus 2pt minus 2pt}  % Adjust space between floats and text
\setlength{\intextsep}{10pt plus 2pt minus 2pt}     % Adjust space between in-text floats and text
\setlength{\floatsep}{12pt plus 2pt minus 2pt}      % Adjust space between floats

% Maximum float dimensions
\renewcommand{\floatpagefraction}{.7}  % Up to 70% of a float page can be floats
\renewcommand{\textfraction}{.1}        % At least 10% of text on a page
\renewcommand{\topfraction}{.8}         % Up to 80% of a top page can be floats
\renewcommand{\bottomfraction}{.7}      % Up to 70% of a bottom page can be floats

% Place floats exactly in the text with the float package
\floatplacement{figure}{H}  % Forces figures to be placed exactly where they appear in the text
\floatplacement{table}{H}    % Forces tables to be placed exactly where they appear in the text

% Title page setup (optional)
\title{\Huge My Custom Title Page}
\author{\Large Author Name}
\date{\today}

\usepackage{listings}
\lstset{
  breaklines=true,
  breakatwhitespace=false,
  basicstyle=\ttfamily\small,
  frame=single,
}

\usepackage{emptypage} % This will suppress the page number on empty pages.

% Remove default behavior to start chapters on a new page
\let\oldchapter\chapter
\renewcommand{\chapter}{\newpage\oldchapter}


\usepackage{titlesec} % Customizes section titles
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries} % Chapter formatting
  {Chapter \thechapter}        % Label
  {0pt}                        % Space before the title
  {\Huge}                      % Space after the title
